# Development and release tooling dependencies
pre-commit>=3.0.0
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0

# Testing
pytest>=7.0.0
pytest-xdist>=3.0.0
pytest-cov>=4.0.0
coverage>=7.0.0

# Profiling (already in main requirements.txt)
# line_profiler>=4.0.0
# memory_profiler>=0.61.0
# psutil>=5.9.0

# Release tooling (installed via scripts if needed)
# git-chglog (Go binary)
# semantic-release (Node.js, optional)
